import 'package:flutter/material.dart';

class ExpirationsPage extends StatelessWidget {
  const ExpirationsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('到期提醒')));
  }
}
